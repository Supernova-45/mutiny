# Next Rosalind work for mutiny

The first scientific return is integrated from commit `0f02bf9`. The original account is building the interactive HHAT experiment view and a browser-local project reviewer. Continue scientific work in `rosalind/`; leave `src/`, generated `public/` assets, and frontend scripts to the original account. Commit and push completed work, then report the SHA.

## 1. Experimental density around W6 and position 8

Use Molecular Structure Viewer to reopen authentic deposited 6UJQ, 6UJO, 6UK2 and 6UK4 in a legitimately bound workspace session. The earlier discovery found 2Fo–Fc and Fo–Fc channels; loading failed because workspace read roots were not bound. Resolve that session setup through supported tools. A denied capability is not permission to bypass it.

Start with one state. Load and visually inspect the local maps around peptide C:6 and C:8. If successful, repeat for the other states. Return bounded local map assets in a browser-readable format when export is supported, original coordinate files or exact source hashes, map headers/grid transforms, coordinate frame, channel, contour value/units, source URL, and a reproducible manifest. Record any clipping/downsampling and export license/attribution requirements. Aim for small local regions, not enormous whole-cell volumes.

Keep maps in the deposited coordinate frame with matching deposited atoms. Do not attach them to the HLA-aligned comparison PDBs without an explicit, verified map transform. The original account can use a separate deposited-frame experimental-support view. Native renders alone are useful evidence but are not interactive map assets.

Report residue-level wwPDB validation for all four states if available. Inspect support for the proposed W6/P8 differences, including alternate conformations and any relevant caveats. Whole-residue RSCC is not side-chain certainty. Distinguish density availability, loaded maps, inspected maps and exported assets. If loading remains blocked after a bounded attempt, deliver other work immediately.

## 2. One real candidate evidence round trip

Use three candidates from the existing HCC1395 fixture. Preserve the exact peptide, allele, reference and variant identities. Through Life Sciences Literature and the sequence/structure viewers where appropriate, return useful evidence for each selected candidate: verified identity/context, exact experimental matches if any, related evidence explicitly labeled as related, or a documented unresolved/no-exact-match finding. A same-gene paper cannot become exact epitope evidence. Do not fabricate response labels or structures.

Supply a request/return fixture with source locations and actual tool receipts. Return no-match results with the search scope and date, not as proof that no evidence exists. No patient data, remote model runs, or new prediction workflows are needed.

## 3. Refine the portable contract

The current full-project fingerprint changes when notes or view state change. Propose and test a versioned replacement: immutable scientific-data identity separate from mutable review state, plus a digest of the exact requested candidate/sequence/HLA/reference contexts. Specify canonicalization, ordering and migration explicitly. Editing a note or camera/view selection must not orphan pending evidence; changing sequence or HLA must prevent stale evidence attachment. Request/response matching remains exact and duplicate imports remain idempotent. Do not silently reinterpret version 0.1.0.

The frontend initially opens normalized project JSON and saves notes/view state as JSON. It preserves the current schema; it will not ship a new evidence-return importer until the refined exchange is agreed. Test note-edit/reopen, changed peptide, changed allele, changed predictor metadata, foreign project, duplicate claim and unresolved-return cases. Preserve human notes and proposed corrections separately from scientific fields.

## Already available; do not redo

The seven Table 1 ligand measurements are ready for the app. W6 analogues and alanine variants have assay data but no additional deposited crystal states. Fixed-HLA W6 ring RMSD and native single-atom displacement remain different measurements. The app derives display endpoints from exact atom identities in its existing display frame and checks them against the returned geometry.

No video, narration, captions, storyboard or player work. Those remain with the original account after the updated app is ready. No whole-Workbench screenshots are required to unblock delivery; keep native renders labeled accurately.
